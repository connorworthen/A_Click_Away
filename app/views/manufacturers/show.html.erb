<table>
  <tr>
    <h2>All Manufacturer</h2>
    <th></th>
    <th></th>
    <th></th>
  </tr>

  <% @manufacturer.each do |m| %>
  <tr>
    <td><%= link_to 'Edit', edit_manufacturer_path %></td>
    <td><%= link_to 'delete', manufacturer_path(@manufacturer), :method => :delete, data: {:confirm => "Are you sure?" } , :title => "Delete #{@manufacturer.name}" %></td>
  </tr>
  <% end %>
</table>

<br />

<td><%= link_to "Create a new manufacturer", new_manufacturer_path(@manufacturer) %></td>